#include <stdio.h>

int main (void)
{
    int x, y;

    scanf("%d %d", &x, &y);//x ve y değeri isitoyurz kullanıcıdan ve aldğımı bu değerleri & işaret,ylr pointerlarda tutuyoruz.
    printf("x: %d y: %d\n", x, y);

    return 0;
}
